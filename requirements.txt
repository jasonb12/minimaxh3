# Install with: uv pip install -r requirements.txt --torch-backend=auto
# (torch needs the CUDA 13 build for Blackwell / sm_120; --torch-backend=auto picks it)
torch==2.13.0
torchvision==0.28.0
# MiniMax-H3 landed in diffusers 0.40.0 (PR #14355 + LoRA loader #14408)
diffusers==0.40.0
transformers==5.14.1
accelerate==1.14.0
peft==0.20.0
torchao==0.17.0
av==18.0.0
gradio==6.22.0
numpy==2.5.1
pillow==12.3.0
ftfy==6.3.1
