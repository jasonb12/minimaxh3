# Install with: uv pip install -r requirements.txt --torch-backend=auto
# (torch needs the CUDA 13 build for Blackwell / sm_120; --torch-backend=auto picks it)
torch==2.13.0
# MiniMax-H3 support is not in a diffusers release yet; pinned to the integration PR
# https://github.com/huggingface/diffusers/pull/14355
diffusers @ git+https://github.com/huggingface/diffusers.git@abc5e9bf71fd38f53cd471bc3acaa84bc5ecbfdc
transformers==5.14.1
accelerate==1.14.0
torchao==0.17.0
av==18.0.0
numpy==2.5.1
pillow==12.3.0
ftfy==6.3.1
